import * as React from 'react';
import { Text, View, StyleSheet, Image } from 'react-native';

export default class AssetExample extends React.Component {
  render() {
    return (
      <View style={assetsStyles.container}>
        <Text style={assetsStyles.paragraph}>
          Hello!
        </Text>
        <Image style={assetsStyles.logo} source={require('../assets/snack-icon.png')} />
      </View>
    );
  }
}

const assetsStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 24,
  },
  paragraph: {
    margin: 24,
    marginTop: 0,
    fontSize: 14,
    fontWeight: 'bold',
    textAlign: 'center',
  },
  logo: {
    height: 128,
    width: 128,
  }
});
